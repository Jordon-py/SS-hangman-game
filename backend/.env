# Local development defaults for backend.
# On Heroku, set these as Config Vars (PORT is provided automatically).
FASTAPI_HOST=0.0.0.0
FASTAPI_PORT=8000
ALLOWED_ORIGINS=["*"]
ALLOWED_ORIGIN_REGEX=https://.*--auralmind\.netlify\.app

MAX_UPLOAD_MB=200
JOB_TIMEOUT_SEC=3600
